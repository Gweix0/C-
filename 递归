void print(int n)	//递归，程序调用自己的编程技巧称为递归  输入123，打印1 2 3
{
	if (n > 9)		//1.递归要存在限制条件  2.每次递归调用之后要越来越接近这个限制条件 
	{
		print(n / 10);			
	}
	printf("%d ", n%10);
}

int main()
{
	int num = 123;
	print(num);
	return 0;
}
